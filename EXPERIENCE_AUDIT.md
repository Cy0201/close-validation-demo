# 体验与操作层级梳理

更新：2026-09-18。范围为 CloseWren 本地项目；相邻 Wren 源码库和其他工具不在本轮修改范围。

## 操作层级

数据源 → 流程（绑定一张检验主表）→ 文件夹中的节点 / 独立聚合卡 → SQL 编辑 → AI 辅助或 Wren 语义与规划 → 应用草稿 → 保存方案 → 运行结果 → 节点聚合 → 分组明细 / 下载。

节点编辑顶部提供返回流程入口；关闭 AI 或 Wren 侧栏回到当前 SQL。Wren 侧栏与 AI 侧栏共用编辑器右侧空间。流程设置调整主表后，需要重新检查 SQL 并保存。标准参考表共享，其他流程主表不可见。

## 本轮已修改

- 恢复流程主表设置入口，加入编辑器返回流程路径。
- 新增 Wren 语义入口及编辑器右侧面板：维护表和字段业务说明，下一次 AI 请求使用已保存说明；可查看当前 SQL 的真实 Wren dry_plan 输出。
- 聚合 SQL 的规划预览明确显示输入节点；完整兼容性仍由保存方案及运行时逐节点检查。
- 管理员密码在服务端校验，仅保存加盐哈希。会话 30 分钟有效，关闭管理窗口主动注销。普通用户无法调用管理员配置、实现查看接口。
- 管理员可调整查询时限、单查询线程数、内存、预览行数。展示模型定义、作用域、引擎版本、执行边界和相关实现源码。只读 / 隔离 / 严格规划属于固定安全约束，展示但不可关闭。
- 节点名称从绝对定位改为网格自然排版，长名称撑开高度，避免上轮换行仍裁切左边和底部的问题。
- 配置面板统一间距、输入框、折叠诊断区域；关闭不改变 SQL；尊重减少动画的系统设置。

## 仍需后续优化

- app.js 存在旧版 renderPlan 装饰器和多轮覆盖样式，应逐步整合为一个渲染入口，不能直接删除而未核对交互。
- 已增加运行节点增量更新并保留横向滚动位置；待用户验收长流程的实际动效。
- bootstrap 携带历史运行结果，可能拖慢初始加载；需要分离历史摘要与按需明细接口。
- AI 澄清、拒绝、继续编辑、重新生成需用户按真实服务做操作验收。
- 本项目集成的是 Wren 本地 SDK，不是 Wren Classic 全套服务。尚未接入可视化关系编辑、计算字段编辑或完整 MDL 写入。业务说明影响 AI 上下文，不会直接重写 Wren 的物理模型。

本轮未运行用户的业务校验；仅做代码语法与服务启停检查。实际 UI、动效、业务结果由用户验收。
