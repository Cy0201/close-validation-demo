# 项目约定

- 每个用户回合开始先列出整体设计和相关细节，再说明本轮工作。
- 结算前与结算后分别独立校验；不要实现或假定前后对比、自动等价映射。
- 用户当前只要求架构、数据接入和可运行 Demo。业务规则由用户以后给出，不预设、不猜测，也不把缺少规则当成通用能力开发的阻碍。
- 手工 SQL 是常驻完整入口。AI 不可用时手工 SQL 与已存规则仍应可运行。
- 参考 README.md、ARCHITECTURE.md 区分已实现与规划内容。原始报表不修改，测试规则不得留在用户状态库。
- 修改执行、数据或状态逻辑时运行 `.venv\Scripts\python.exe -m unittest test_demo -v`；界面变更用本地浏览器验证。
