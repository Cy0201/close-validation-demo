# 本轮验证记录

2026-09-16，本机 Python 3.12.14、WrenAI 0.14.0、wren-core-py 0.7.6、DuckDB 1.5.5。

`python -m unittest test_demo -v`：8 项通过，14.295 秒。测试规则与运行记录使用独立临时 SQLite 库，没有留在用户状态库。

1. 两份真实数据全量计数分别为 139,203 与 252,391，原始字段结构独立。
2. 中文期间字段聚合可执行；金额返回为精确十进制字符串。
3. 结算后查询仅预览 7 行时，完整查询结果数仍为 252,391。
4. 写 SQL、多语句、外部文件函数、系统表及跨库查询均被拒绝。
5. 非本站 Origin 请求被拒绝。
6. 规则保存、独立运行、历史留存、删除规则后保留运行记录可用。
7. 非法数据集及仅存在于另一空间的字段被拒绝。
8. 常规 CTE 和字符串中的分号可以正常查询，不被简单关键字检查误伤。

浏览器实测：首页真实元数据和待配置进度可见；两空间切换时字段目录和查询草稿独立；分别执行按期间计数，返回 139,203 和 252,391；SQL 编辑器、结果表及 Wren 展开 SQL 入口可见。

启动/停止/再次启动均已验证；再次启动按文件 hash 复用导入数据。原始文件没有修改。

尚未配置真实 AI 模型，因此没有验证模型调用及生成准确率。业务规则、业务复核通过条件和多人权限不在本轮验证范围。
